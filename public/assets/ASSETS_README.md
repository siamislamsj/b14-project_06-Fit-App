Place the provided image files in this folder:

- `banner.png` — used on the home page hero.
- `logo.png` — used in the navbar and footer.

The app loads them from `/assets/banner.png` and `/assets/logo.png`.
